const controller = 'dashboard';

export const dashBoardController = {
  tag: {
    controller,
    action: 'tag',
    method: 'GET'
  },
  event: {
    controller,
    action: 'event',
    method: 'GET'
  }
};
